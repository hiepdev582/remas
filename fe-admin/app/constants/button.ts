export enum ButtonSize {
  LARGE = "large",
  MIDDLE = "middle",
  SMALL = "small",
}

export enum ButtonType {
  PRIMARY = "primary",
  GHOST = "ghost",
  DASHED = "dashed",
  LINK = "link",
  TEXT = "text",
  DEFAULT = "default",
}
