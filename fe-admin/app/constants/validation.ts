//#region Constants
const MIN_CHAR = 1;
//#endregion

//#region Regex
const hasLowerCase = /[a-z]/;
const hasUpperCase = /[A-Z]/;
const hasNumber = /[\d]/;
const hasSpecialChar = /[^a-zA-Z\d]/;
//#endregion

export { MIN_CHAR, hasLowerCase, hasUpperCase, hasNumber, hasSpecialChar };
