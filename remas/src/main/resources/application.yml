spring:
  application:
    name: remas
  datasource:
    url: jdbc:mysql://localhost:3307/remas?useSSL=false&serverTimezone=UTC
    username: root
    password: Zz0973300Zz@
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
app:
  jwt:
    secret: ${JWT_SECRET}
    access-token-expiration-ms: ${JWT_ACCESS_TOKEN_EXPIRATION_MS}
    refresh-token-expiration-ms: ${JWT_REFRESH_TOKEN_EXPIRATION_MS}
